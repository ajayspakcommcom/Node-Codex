console.log(
  JSON.stringify({
    alertName: "checkout_error_budget_burn",
    actionable: true,
    owner: "checkout-team",
    responderAction: "Inspect checkout latency and dependency failures using linked dashboard and traces",
  }),
);
