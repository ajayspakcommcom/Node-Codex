console.log(
  JSON.stringify({
    reviewQuestions: [
      "Did this alert page when no human action was needed",
      "Did responders have enough context to act",
      "Has this alert repeated without tuning",
    ],
    note: "Alert quality must be reviewed after incidents and noisy false positives.",
  }),
);
