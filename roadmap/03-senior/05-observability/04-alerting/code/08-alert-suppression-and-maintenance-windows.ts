console.log(
  JSON.stringify({
    alertName: "deployment_error_rate",
    suppressionRule: "suppress during approved maintenance window for service",
    reason: "Avoid noisy pages during known disruptive operations without hiding real incidents permanently.",
  }),
);
