# Governance Notes

- apply operations should be restricted and audited
- production changes should require reviewed plans
- state backends must be encrypted and locked
- credentials for Terraform execution should use short-lived access where possible
- manual infrastructure changes should be treated as drift and reconciled back into code
