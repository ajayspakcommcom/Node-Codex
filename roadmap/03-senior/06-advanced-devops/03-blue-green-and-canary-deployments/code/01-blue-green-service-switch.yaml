apiVersion: v1
kind: Service
metadata:
  name: checkout-api
  namespace: checkout-prod
spec:
  selector:
    app.kubernetes.io/name: checkout-api
    rollout-slot: green
  ports:
    - name: http
      port: 80
      targetPort: http
