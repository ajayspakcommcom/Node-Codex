{
  "name": "@packages/shared-config",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint shared-config",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo test shared-config"
  }
}
