const maintainabilityPatterns = [
  "Keep routing and residency policy in explicit control-plane modules.",
  "Review failover as a policy decision, not an operator shortcut.",
  "Make regional rollout sequencing testable and auditable.",
  "Treat replication lag as a release gate, not an afterthought.",
];

console.log(maintainabilityPatterns);
