export const maintainabilityPatterns = [
  "Define chaos experiments as bounded reviews, not ad hoc disruptions.",
  "Keep steady-state and abort thresholds explicit before every run.",
  "Track resilience gaps as follow-up work after each experiment.",
  "Increase blast radius only after smaller experiments prove safe.",
] as const;
