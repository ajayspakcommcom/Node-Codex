export const maintainabilityPatterns = [
  "Map every compliance requirement to a real technical control owner.",
  "Generate evidence from system events instead of manual audit assembly.",
  "Keep retention and deletion policy explicit for regulated datasets.",
  "Treat compensating controls as time-bounded exceptions with review dates.",
] as const;
