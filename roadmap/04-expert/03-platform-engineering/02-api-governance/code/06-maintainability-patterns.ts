export const maintainabilityPatterns = [
  "Register ownership for every externally consumed contract.",
  "Classify changes explicitly instead of arguing about impact informally.",
  "Automate style and compatibility checks in CI where possible.",
  "Require deprecation notice and rollout planning before risky removals.",
] as const;
