export const maintainabilityPatterns = [
  "Version templates and publish upgrade paths instead of forcing silent changes.",
  "Keep platform controls explicit and reviewable rather than hidden in shell scripts.",
  "Make exceptions time-bounded with an owning approver.",
  "Measure adoption and drift so the paved road stays relevant.",
] as const;
