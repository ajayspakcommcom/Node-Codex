/*
1. Replace a || fallback with ?? in a config parser.
2. Convert a mutating object update into an immutable spread update.
3. Refactor a callback-heavy function into async/await.
4. Split one file into small modules with explicit exports.
5. Replace deep property access with optional chaining plus validation.
*/

export function exerciseOne(): never {
  throw new Error("Implement exercise one");
}

export function exerciseTwo(): never {
  throw new Error("Implement exercise two");
}

export function exerciseThree(): never {
  throw new Error("Implement exercise three");
}
