/*
1. Show the execution order of sync code, a promise callback, and a timer.
2. Build a blocking loop example and measure the timer delay.
3. Compare process.nextTick with a promise microtask and setTimeout.
4. Refactor a CPU-heavy request-path example into a queued or background-job pattern.
5. Add simple concurrency control to a list of external calls.
*/

export function exerciseOne(): never {
  throw new Error("Implement exercise one");
}

export function exerciseTwo(): never {
  throw new Error("Implement exercise two");
}

export function exerciseThree(): never {
  throw new Error("Implement exercise three");
}
