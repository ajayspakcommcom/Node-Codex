# Package Manager Examples

These sample files are intentionally small.

They demonstrate:

- a clean service `package.json`
- a simple workspace root `package.json`
- dependency categorization
- team-friendly scripts

This topic is documentation-first, so these examples are meant to support the concepts rather than become a full code implementation.
